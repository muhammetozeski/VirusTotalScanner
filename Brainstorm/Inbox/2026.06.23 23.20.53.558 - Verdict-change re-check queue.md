# Verdict-change re-check queue

Separate menu. Pin chosen hashes, re-query by sha256 on a backoff schedule (keyless GUI), notify only when the verdict CHANGES. One question for the whole batch (not per file). Period configurable per-file and a default in settings.

_Queued from the unfinished list + approved second-brainstorm items (2026.06.23 23.19)._

