# Click column headers to sort the queue (name / size / status / severity)

Kategori: ux | Efor: M | Rank: 1

## Ne
ScanQueueControl.ConfigureGrid (root copy: .claude/worktrees/autoloop/ScanQueueControl.cs ~line 455-465) binds the DataGridView to _scheduler.Items / _view with AutoGenerateColumns=false and has NO ColumnHeaderMouseClick handler, so the four headers (Dosya/Boyut/Durum/İlerleme) are inert and the queue stays in arrival order. Add a ColumnHeaderMouseClick handler that always routes through the existing _view BindingList (force a sorted snapshot of _scheduler.Items into _view even when no filter chip is active, so the live Items order and BulkAdd are never reordered under the running Parallel loop), re-sorts with a stable per-column comparer, paints a ▲/▼ glyph on the clicked header, toggles direction on re-click. Give Durum a synthetic 'severity' key from Report.DetectionCount (threatiest first, then IsMalicious). Re-apply the active sort on each _repaintTimer tick (line 137) so newly-finished rows slot in.

## Neden
After a big sweep the only severity navigation is the J/K hop or eyeballing red rows; you cannot float the worst file to the top, sort by size to spot a suspicious 80MB 'codec', or cluster failures. Header-click sort is the single most expected list interaction and it is currently dead, turning a scroll-and-hunt grid into one-click triage. The _view/filter plumbing it needs already exists.
