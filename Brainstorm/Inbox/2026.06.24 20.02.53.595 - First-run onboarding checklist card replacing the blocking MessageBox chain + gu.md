# First-run onboarding checklist card replacing the blocking MessageBox chain + guided zero-history empty state

Kategori: ux | Efor: M | Rank: 11

## Ne
In ScanOverviewControl.cs add a dismissable 'Başlangıç' card (shown while !FirstRunCompleted or until every step is checked) listing setup steps as live ✓/✗ rows with inline action buttons, reusing the existing CoverageRow layout (line 151-164): add an API key / turn on keyless mode (AppServices.Rotator.HasUsableKeys), install the right-click menu (ContextMenuInstaller.Verify), turn on download-watching (Settings.WatchDownloads), run a first scan (ScanHistoryStore.All().Count > 0); each row self-updates in Refresh2. Demote MainForm.RunFirstRunWizard so it stops popping the blocking confirm chain and just marks FirstRunCompleted. Additionally, in Refresh2/BuildTiles when history is empty, hide the three '0' count tiles (which read as broken to a newcomer) and show a one-line 'what this app does' explainer plus a single primary CTA ('İlk taramanı yap → İndirilenleri tara').

## Neden
A brand-new user is currently hit by a stack of blocking yes/no dialogs and then dropped onto an overview whose three big '0' tiles read as broken. An always-visible checklist plus a guided empty-state lets them finish setup at their own pace, see what is still missing, act on each item in one click, and have one obvious first action — reusing the CoverageRow and tile UI already in the file.
