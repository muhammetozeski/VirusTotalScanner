# Preserve the action on threats deferred during quiet hours instead of a bare count

Kategori: protection | Efor: M | Rank: 2

## Ne
Replace the int _suppressedToasts counter (MainForm.cs:118) with a List<DeferredToast> capturing each held toast's title/text/icon AND its ToastAction + payload. In the _toastReplay.Tick handler (MainForm.cs:247-258) if exactly one threat was deferred re-raise it with its original ShowThreat action so clicking still jumps to the file via _scan.FocusItem; if several show 'N tehdit bulundu - incele' tagged to open the pre-filtered queue/History. Drop the information-losing 'Sessiz mod sirasinda N bildirim ertelendi' line (MainForm.cs:255).

## Neden
Today a real threat caught during quiet hours or fullscreen (Gated, MainForm.cs:123-129) collapses into a non-actionable count, throwing away the path, verdict, and click-to-act so the user must re-hunt it by hand. Preserving the action makes the quiet-hours gate safe to actually leave on instead of trading protection for quiet.
