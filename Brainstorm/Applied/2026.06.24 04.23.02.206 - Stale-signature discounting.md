# Stale-signature discounting

Cost: M

Use each detecting engine's signature-DB date (engine_update) — parsed into VtEngineDto.EngineUpdate but DROPPED in MapReport, never reaching VtEngineResult. Add EngineUpdate/UpdatedUtc to VtEngineResult, populate in MapReport (yyyymmdd). Per report, count detections from engines whose DB is older than a threshold (e.g. 60 days) vs the file's last-analysis time. Show 'N of M detections come from engines whose signatures are 4+ months old — likely stale generic hit' and a stale-adjusted count. Orthogonal to major/minor (reputation) and signature-vs-heuristic (method). Threshold in settings. Recompiled clean binaries trip months-old generic sigs a since-updated engine no longer fires.

_From round-3 autonomous brainstorm (10 proposer-vs-critic groups, 2026.06.24 04.2x)._

