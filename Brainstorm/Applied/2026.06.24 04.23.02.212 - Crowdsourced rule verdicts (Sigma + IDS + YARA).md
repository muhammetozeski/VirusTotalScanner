# Crowdsourced rule verdicts (Sigma + IDS + YARA)

Cost: S

Surface VT's crowdsourced detection-RULE signals already in the captured /ui/files/<hash> JSON but thrown away: sigma_analysis_results (rule name + severity), crowdsourced_ids_results (Suricata/Snort/ET alert names), crowdsourced_yara_results (YARA rule + author/source). Add three nested DTO lists to VtFileAttributes, project through MapReport into new VtFileReport fields, render a 'Community rules' section in the detail pane (rule name, severity/category, ruleset author). Optionally feed rule-hit count into the recommendation. Zero new fetch/quota — the bytes are already in hand. Names WHY a file is flagged (e.g. 'Suricata ET MALWARE Agent CnC Checkin', 'YARA win_redline_stealer'), fires on low-AV-count files. Cost S.

_From round-3 autonomous brainstorm (10 proposer-vs-critic groups, 2026.06.24 04.2x)._

