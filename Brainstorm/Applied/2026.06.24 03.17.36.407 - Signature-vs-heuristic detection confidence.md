# Signature-vs-heuristic detection confidence

Use VtEngineResult.Method (already parsed at VtApiClient ~176, currently unused). Classify each detection as signature (method=='blacklist') vs heuristic/ML (everything else). Add VtFileReport.SignatureHits/HeuristicOnlyHits/HeuristicOnly (DetectionCount>0 and zero blacklist). Surface in ConsensusText: 'Tum tespitler sezgisel/ML (imza yok -> olasi FP)' vs 'N imza eslesmesi (gercek)'. Optionally let --fail-on treat heuristic-only as non-failing. Store the two ints in cache like MajorFlaggers. Strongest single FP signal VT exposes, already in hand. Cost S.

_From round-2 autonomous brainstorm (10 proposer-vs-critic groups, 2026.06.24 03.1x)._

