# Detection-name normalization

Aggregate per-engine detection strings into a single best-guess family/type label (e.g. 'likely: CoinMiner / PUA'). Optional; default still shows ALL engine results.

_Queued from the unfinished list + approved second-brainstorm items (2026.06.23 23.19)._

