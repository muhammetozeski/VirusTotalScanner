# Segmented live progress bar showing malicious/suspicious/clean/skipped proportions

Kategori: ux | Efor: M | Rank: 2

## Ne
The bottom _overall is a flat ProgressBarStyle.Continuous and OnProgress only sets _overall.Value plus a text summary, even though OverallProgress (ScanModels.cs:107-116) already carries Total/Done/Malicious/Suspicious/Clean/SignedSkipped/Failed. Replace it with an owner-drawn Panel that on each OnProgress draws stacked colored segments (Theme.Danger=Malicious, Warning=Suspicious, Success=Clean, SubtleText=SignedSkipped, hatched tail for Total-Done not-yet-scanned) sized from those counts. Keep the ETA/files-per-sec/elapsed line beneath, and wire the existing tooltip so hovering a segment shows its count.

## Neden
The bar throws away the per-verdict breakdown it is already handed and shows one undifferentiated fill. A red sliver appearing while thousands of files are still pending answers 'are there threats in this batch yet?' at a glance — far louder than a buried Zararlı number in the summary text, and it costs the user zero reading mid-scan.
