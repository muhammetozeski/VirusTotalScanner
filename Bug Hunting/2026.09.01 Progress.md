# Bug Hunt — İlerleme (2026-09-01)

## Faz 1 — Kanıtlı çekirdek bug'lar: TAMAMLANDI

| # | Bug | Durum | Commit |
|---|-----|-------|--------|
| 1 | Mark checkbox kolonu her repaint'te DataError (18.723 log/oturum) | Düzeltildi — null hücre değeri `NullValue=false` ile unchecked'e eşlenir. A/B testi: eski kod 12 DataError, yeni kod 0. | 6cefbf5 |
| 2 | "Zararlı dosya hâlâ diskte" banner'ı allowlist/klasör-bastırma tanımıyor; eski threat kaydı yeni temiz kaydı eziyor | Düzeltildi — `ScanHistoryStore.FirstLiveThreat()`: path başına en yeni kayıt + allowlist + folder-suppression süzgeci; banner Allowlist/FolderSuppression değişiminde canlı yenilenir. 3 senaryo snapshot ile doğrulandı. | 3a65ae2 |
| 3 | Tarama sürerken gelen istekler sesizce yok ediliyordu (bugün 5 kez) | Düzeltildi — istekler bekleyen kuyruğa; mevcut tur bitince otomatik ek tur (grid'i temizlemeden ekler); Cancel bekleyenleri de iptal eder; durum satırında "N öğe kuyruğa eklendi" mesajı. | e29d969 |
| 4 | Sağ tık menüsü kuruluyken kartlar "kurulu değil" gösteriyordu (elevated süreç config yarışı) | Düzeltildi — tek gerçek kaynak registry (`Verify()`); `ContextMenuInstalled` ayarı silindi; elevated süreç config'e artık hiç yazmıyor; startup'taki modal "onarılsın mı?" sorusu yerine coverage kartında pasif "eski kuruluma işaret ediyor [Onar]" satırı. | ca7b0f6 |
| 5 | Decrypt edilemeyen KeyVault blob'u bir sonraki kayıtta boş vault ile eziliyordu (bugün yaşandı) | Düzeltildi — blob `keyvault-backup-<zaman>.txt`'ye yedeklenir (DataFolder→%TEMP% sıralı deneme); vault boşken decrypt-hatalı blob'un üstüne yazılmaz; kullanıcı key eklerse bilinçli yeni vault normal yazılır. 3 senaryo canlı doğrulandı. | e6c3862 |
| 6 | ~~İki instance aynı anda çalışıyor (SingleInstance yarışı)~~ | **Yanlış çıkarımdı.** Loglar netleştirdi: 14.14 oturumu 14:17:46'da bitti, 14.17 oturumu 14:17:52'de başladı — üst üste çalışma yok, mutex doğru; ben log dosyası/LastWriteTime eşleştirmesini yanlış okumuştum. Gerçek olay: crash → yeniden açış. | — |
| 6b | **Yeni bulgu:** Filtre chip'ine tıklayınca çökme — `IndexOutOfRangeException "Index 25 does not have a value"` (oturumda 8 kez; kullanıcıyı uygulamayı kapatmaya mecbur etti) | Düzeltildi — CurrencyManager'ın bayat pozisyonu `CurrentRow` getter'ını patlatıyordu; 19 kullanım noktasının tamamı merkezi `EntityGrid.CurrentItem<T>()`/`CurrentIndex()` güvenli erişimcilerine bağlandı. | 08d1a16 |

Decrypt hatasının (#5'i tetikleyen) en olası kökü: 14:17:46'daki çökme sırasında `AtomicFile`'ın
`File.Replace` başarısız olup `File.Copy` fallback'ine düştüğü anda yarım kesilen config yazımı.
Koruma katmanı (#5) bu durumu artık veri kaybı olmadan atlatıyor.

## Faz 5'e taşınan görsel notlar
- Overview banner'ının sağındaki eylem butonu ("Geçmişi aç") snapshot'ta görünmüyor — denetlenecek.
- CoverageRow etiketi 240px sabit; "Sağ-tık menüsü (eski kuruluma işaret ediyor)" kırpılıyor.
- QuarantineVaultDialog.cs:62'de CS4014 uyarısı (beklenmeyen async çağrı) — Faz 2 review'da ele alınacak.

## Sıradaki: Faz 2 — sistematik kod gözden geçirme (124 dosya, katman katman)
Sonuçlar `Bug Hunting\Code Review.md`'ye işlenecek.
